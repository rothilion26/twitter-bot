# 🚀 X/Twitter Like Automation Bot
Professional, script-driven automation for liking tweets on X (Twitter), built using **Node.js** and **Playwright**.

🔹 Automated browser workflow — no API access required
🔹 Minimal setup — guided PowerShell installer
🔹 Human-like rate limits and interaction patterns
🔹 Session-based authentication stored securely on device

> ⚠️ **Important Notice**
> Automating actions on X/Twitter may violate their Terms of Service.
> Account restrictions, suspensions, or bans are possible.
> Proceed at your own risk.

---

## ✅ System Requirements

- Windows 10 or Windows 11
- A valid X/Twitter account

> All required components (Node.js, npm packages, Chromium browser, etc.) are automatically installed during setup.

---

## 🔧 Setup Instructions

### Step 1 — Extract Package
Extract the ZIP archive to any local directory, e.g.:

```
C:\Users\YourName\Desktop\twitter-bot\
```

### Step 2 — Automated Installation
Right-click `bootstrap.ps1` and select:

➡ **Run with PowerShell**

This script will:

- Install Node.js (if not present)
- Install required dependencies
- Download the browser environment
- Configure `.env` automatically
- Enable one-click launching via `run_bot.bat`

> ⚠️ Execution Policy Issue  
> If PowerShell reports that scripts are blocked, run as Administrator and execute:
> ```powershell
> Set-ExecutionPolicy Unrestricted -Scope Process
> ```

---

## 🔐 Step 3 — Save Login Session

Run once to authenticate:

```powershell
npm run save-session
```

A browser will open → log into X → return to terminal and press **ENTER**.

This will generate a secure `state.json` session file.

> 🔒 Treat `state.json` as confidential. It contains your login cookies.

---

## ✍️ Step 4 — Define Targets (`input.csv`)

Edit the CSV file to provide one target URL per line:

**User profile**
```
https://x.com/username
```

**Live tweet search**
```
https://x.com/search?q=from:username&f=live
```

The bot will process each entry sequentially.

---

## ▶️ Step 5 — Run the Bot

Most convenient method:

➡ Double-click `run_bot.bat`

Or run manually with custom parameters:

```powershell
node src/bot.js --tweets=3 --rows=1 --max-scan=150 --headless=false
```

---

## ⚙️ Command-Line Options

| Parameter | Default | Description |
|----------|---------|-------------|
| `--tweets` | 50 | Maximum likes per user/profile |
| `--rows` | 1 | Number of rows (targets) to process |
| `--max-scan` | 500 | Maximum tweets to evaluate |
| `--daily-cap` | 300 | Global daily like limit |
| `--headless` | false | Enable/disable visible browser mode |

Example:
```bash
node src/bot.js --tweets=20 --rows=2 --headless=true
```

---

## 📄 Logs & Monitoring

Activity history is stored in:

```
bot.log
```

Live monitoring:
```powershell
Get-Content bot.log -Wait
```

---

## 🧯 Troubleshooting

| Issue | Resolution |
|------|------------|
| `npm` command not found | Rerun `bootstrap.ps1` to install Node.js |
| Login not detected | Rerun `npm run save-session` |
| Bot performs zero likes | Increase `--max-scan` / verify search URLs |
| “Not logged in” error | Delete `state.json` → save session again |

---

## 📁 Project Structure

```
twitter-bot/
├── src/
│   ├── bot.js
│   ├── save_session.js
│   └── helper.js
├── bootstrap.ps1
├── run_bot.bat
├── input.csv
├── .env.example
├── state.json   (generated after session save)
├── processed.json   (generated by bot)
├── bot.log     (generated by bot)
└── README.md
```

---

## 🔒 Security Guidance

- Do **not** share `state.json` with anyone
- Do **not** sync it to cloud storage
- Avoid using the same machine for high-risk tasks
- Excessive activity can trigger platform defenses

The bot includes safety limits, but user discretion is advised.

---

## 📝 License
Distributed under the **ISC License**.
Open to contributions — pull requests are welcome.

---

Thank you for using this automation tool.
If you encounter issues or have feature requests, please reach out via issues.

> Automate responsibly — and stay secure. ⚡
